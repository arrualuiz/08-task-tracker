// Copie este arquivo para "config.js" e preencha com os seus valores reais.
// config.js fica no .gitignore para você não commitar o token por engano.

const CONFIG = {
  APPS_SCRIPT_URL: 'https://script.google.com/macros/s/SEU_DEPLOY_ID/exec',
  TOKEN: 'SEU_TOKEN_SECRETO_AQUI'
};
